```css
display: none; 隐藏元素 不保留位置
visibility: 保留元素
overflow: none; 超出是否切割
```

```css
cursor:更改鼠标样式
outline:none 取消轮廓线
resize:none 防止用户拖拽 文本域
vertical-align 垂直对齐  只针对行内元素和行内块元素
text-flowover: ellipsis  不全的文字显示...
white-space:nowrap 只显示一行  不自动换行
overflow:hidden 超出切割
```

```css
滑动门图片
a负责左侧 左对齐
span负责右侧  右对齐
利用padding撑开
<a><span></span></a>
```

字体图标

